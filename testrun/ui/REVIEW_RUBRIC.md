# UI Redesign — Review Rubric (Stage 3)

Reviewer must evaluate ONLY with direct evidence (screenshots, computed styles, curl/CDP, pytest). No claims from STAGE2 reports trusted without verification.

| # | Area | Acceptance criteria |
|---|---|---|
| A | Design fidelity | Dark graphite studio surfaces; single Apple-blue accent (#0071e3) on CTAs/play/active/playhead; frosted-glass topbar (blur); NO CRT remnants: no green #33ff66 text, no amber #ffb000 accents, no scanlines/vignette/grain/flicker rendering; radii 8/12/18/980; mono tabular timecodes. Verify by pixel-sampling screenshots + reading style.css. |
| B | Logo (mission-critical) | Logo reads "Open": wordmark "open Edit" with a mark in the O position. The mark is a MONITOR/SQUIRCLE that IS the letter O (rounded-rect screen, bezel, gradient, glow). NO literal "O" character inside the monitor (inspect DOM: logo-mark has no text node "O"; .crt-tv-screen hidden). Optical size ≈ capital O of the wordmark (mark 22px vs wordmark ~16px font — ratio ≈ 0.72–0.75). Screenshot crop of the topbar + DOM inspection. |
| C | Functionality | ALL inventory features work against the live server with a selected project: project select/create/refresh; assets list + upload dropzone + preview modal + Range streaming; edit graph list + undo/delete + detail; renders list + proxy/final buttons + polling; notes add-at-playhead + list; chat widgets present and review-gated; prompt chips; WS reconnect; preview player streams render + asset (206); NEW transport (skip-back/play/skip-fwd + timecode) works; timeline ruler/scrub/zoom/fit/markers/timecode-copy; cmd-K; theme toggle (SVG icon swap, data-theme flips); panel toggles; settings modal; toast; conn status. |
| D | Layout & responsive | At 1600x1000 with left panel collapsed (review-only default): grid = 0/1fr/260, center preview ~1340px wide with video, right rail 260px. At 1440/900/800: center preview stays visible, no zero-width tracks, no rail swap, mobile stack correct. Verify via CDP computed styles. |
| E | Quality gates | `cd /home/amr/apps/mlt-pipeline && source .venv/bin/activate && python -m pytest tests/ -q --timeout=120 -p no:cacheprovider` → exit 0, 0 failures (~1600 pass, 7 env skips). Browser console: no app JS exceptions; only favicon 404 acceptable. Contract: all 68 IDs from testrun/ui/CONTRACT.md still present in index.html; JS-emitted classes unchanged; 3 inline var names (--green/--text-dim/--border) defined. |
| F | Verdict | Per-item PASS/FAIL/UNCERTAIN with evidence; overall confidence 0–100; "VERDICT: PASS" ONLY at 100% with zero concerns. Any concern → list fix + re-verify method. |

Evidence files: screenshots testrun/ui/shots/*.png, /tmp/ui_v4_cdp.png, /tmp/ui_v3_main.png; source open_edit/serve/static/{index.html,style.css,app.js}; live server http://127.0.0.1:8000.
