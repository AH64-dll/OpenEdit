## What changed

<!-- One or two sentences: what this PR does. -->

## Why

<!-- The problem this solves, or the feature this adds. Link an issue if one exists. -->

## How it was verified

<!-- What you ran and what you saw. Examples: pytest on the changed tests, a
local render through the MCP server, `bash -n install.sh`. CI runs on every
PR and must pass. -->

## Checklist

- [ ] Focused change (one logical change per PR)
- [ ] CI test set passes locally (`pip install -e ".[mcp,dev]"` + `pytest`)
- [ ] No secrets, keys, or credentials added
- [ ] Docs updated if behavior changed (README / INSTALL / docs/ as applicable)
