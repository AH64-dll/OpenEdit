"""Phase 4.5: skills — pure-Python edit-suggestion logic.

Each module in this package exposes a small set of functions that the
agent can call (directly via run_python, or via the repointed tool
wrappers) to compute proposed edits. Skills are deterministic and
side-effect-free; the agent decides whether to apply them as IR ops.
"""
