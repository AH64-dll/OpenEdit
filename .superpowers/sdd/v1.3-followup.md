# v1.3 followup — chat UI backend integration

**Status:** DONE in commit (follows v1.2.1, on main at HEAD after this note).

## What was done

Integrated the chat-UI backend (`open_edit/serve/`) that the user's external
model produced. The user's model generated ~2800 lines of FastAPI + WebSocket
+ agent-loop + frontend code, but it had three critical mismatches with the
real Open Edit codebase. We fixed all three.

## Mismatches found and fixed

### 1. Schema mismatch (CRITICAL)

The generated `serve/projects.py` queried tables that don't exist in the
real Open Edit schema:

- Generated: `assets (hash, filename, duration)`, `ops (id, type, payload, ...)`,
  `effects`, `review_notes (...)`
- Real (`open_edit/storage/schema.sql`): `edits (edit_id, parent_id, kind, author, timestamp, status, sequence_num, payload)`, `project_meta`, `jobs`
- Real notes (`open_edit/storage/notes.py:80-100`): `notes (note_id, project_id, anchor_type, anchor, text, source, status, ...)`
- Real assets: CAS filesystem layout at `.open_edit/assets/<prefix>/<hash>` with sidecar `<hash>.meta.json` (no SQL table)

**Fix:** Rewrote `serve/projects.py` to use the real storage classes:
`AssetStore` (filesystem), `EditGraphStore.load_all()` (ops), `NotesStore.list_all()`
(notes). Dropped the invented SQL queries and the `_bootstrap_minimal_db`
fallback that created wrong tables.

### 2. Invented tools (HIGH)

The generated `serve/tool_schemas.py` listed 12 real tools + 1 virtual.
Only 10 real tools exist (`open_edit/agent/tools/pyagent_*.py`):

- ✅ 10 real: `add_marker, analyze_narrative, generate_visual_for_segment, get_pending_notes, get_style_profile, place_sfx, propose_silence_cuts, run_python, select_music, set_pinned_value`
- ❌ 2 invented: `get_asset_info, list_assets` — `agent.py:169-171` would have raised `RuntimeError: tool not found` when the LLM called them.

**Fix:** Removed the 2 invented tools. Now 11 tools (10 real + 1 virtual
`trigger_render`).

### 3. IR model mismatch (MEDIUM)

The generated `serve/tool_schemas.py:_IR_MODEL_SUMMARY` described a 4-op
model (AddClipOp, AddEffectOp, RemoveOp, UpdateOp). Real Open Edit has
**24 op kinds** in `open_edit/ir/types.py`.

**Fix:** Rewrote the IR summary to enumerate all 24 real op kinds, their
payload shapes, and the common fields every op inherits from `Operation`.

## Files added

- `open_edit/serve/__init__.py` (1 line)
- `open_edit/serve/projects.py` (450 lines, rewritten)
- `open_edit/serve/llm.py` (308 lines, unchanged from model)
- `open_edit/serve/tool_schemas.py` (380 lines, edited — removed 2 tools, rewrote IR summary)
- `open_edit/serve/agent.py` (388 lines, unchanged from model)
- `open_edit/serve/app.py` (372 lines, unchanged from model)
- `open_edit/serve/_cli_patch.py` (122 lines, reference; the real wiring is in `cli.py`)
- `open_edit/serve/static/index.html` (144 lines, unchanged)
- `open_edit/serve/static/style.css` (559 lines, unchanged)
- `open_edit/serve/static/app.js` (1042 lines, unchanged)
- `open_edit/tests/test_serve_projects.py` (rewritten, uses real storage classes)
- `open_edit/tests/test_serve_agent.py` (5 tests, unchanged — mocks the LLM)
- `open_edit/tests/test_serve_ws.py` (6 tests, unchanged — uses FastAPI TestClient)

## Files modified

- `open_edit/pyproject.toml`: added `fastapi`, `uvicorn[standard]`, `anthropic`, `python-multipart` to `dependencies`; added `pytest-asyncio`, `httpx` to `dev`; added `serve` extra; added `openai` as optional extra.
- `open_edit/open_edit/cli.py`: added `serve` subcommand (`cmd_serve` + parser).

## Test results

**Before integration:** 310 passed, 5 skipped (bwrap in dev env).

**After integration:** 327 passed, 5 skipped. The 17 new tests are:
- `test_serve_projects.py`: 6 tests (real-schema integration)
- `test_serve_agent.py`: 5 tests (agent loop with mocked LLM)
- `test_serve_ws.py`: 6 tests (WebSocket protocol with FastAPI TestClient)

## End-to-end smoke test

`open_edit serve --port 8765`:
- `GET /api/projects` → 200, `[]`
- `GET /` → 200, serves `index.html`
- `WS /api/chat/nonexistent` → connects, sends `{"type":"error","message":"project not found: nonexistent"}`, closes

## What v1.3+ work remains (optional)

1. **Tauri GUI / desktop app** — wrap the static frontend in a Tauri shell for
   a native app experience. The `open_edit/serve/static/` already contains
   the SPA; Tauri can serve it from `tauri://` and proxy `/api` to localhost.
2. **Auth + multi-user** — currently every project is wide-open on the host.
   v2 feature; add token-based auth in front of the FastAPI app.
3. **Real LLM end-to-end test** — the agent loop is tested with a mocked
   LLM. To verify against a real Claude/OpenAI, set `OPEN_EDIT_LLM_API_KEY`
   and run a `run_python` tool through a real chat session.
4. **`act`-based CI** for the serve package (optional `bwrap-tests` style
   job in `.github/workflows/ci.yml`).
5. **Asset streaming URL** — the frontend's asset preview modal doesn't
   actually play the asset (it clears the `<video>` src). Add
   `GET /api/projects/{id}/assets/{hash}/stream` to serve the file with
   `Content-Type: video/mp4`.
6. **Render polling UI** — the right panel has a renders list, but the
   `pollRenderJob` JS polls `/render_jobs/{id}` which the server implements.
   Wire the success/fail toast to appear on completion.
7. **Conversation list** — `conversations/{conv_id}.jsonl` is persisted
   per project but not surfaced in the UI. Add a "past conversations"
   dropdown in the chat input area.

## Files

- `open_edit/serve/` — new
- `open_edit/tests/test_serve_*.py` — new
- `open_edit/open_edit/cli.py` — modified (added `serve` subcommand)
- `open_edit/pyproject.toml` — modified (added deps)
